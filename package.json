{
  "name": "opcua-simulator",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/server",
    "packages/client"
  ],
  "scripts": {
    "dev": "concurrently -k -n SERVER,CLIENT -c cyan,green \"npm run dev -w packages/server\" \"npm run dev -w packages/client\"",
    "build": "npm run build -w packages/server && npm run build -w packages/client",
    "start": "npm run start -w packages/server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "overrides": {
    "hexy": "0.2.2"
  }
}
